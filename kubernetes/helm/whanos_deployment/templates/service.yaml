apiVersion: v1
kind: Service
metadata:
  name: {{ include "whanos_deployment.fullname" . }}
  labels:
    {{- include "whanos_deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whanos_deployment.selectorLabels" . | nindent 4 }}
